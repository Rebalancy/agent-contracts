# Deployment Tools

Set of utilities to deploy the Rebalancer protocol to different networks.

Supported networks:

- Arbitrum Sepolia

## Setup

Run `./1-init.sh` 

# Deploy

Run `./2-deploy`

<!-- TODO: Perhaps migrate to a `justfile` -->